import { FC } from "react";

import styles from "./CartCard.module.scss";

const CartCard: FC = () => {
    return (
        <article>
            
        </article>
    );
};

export default CartCard;